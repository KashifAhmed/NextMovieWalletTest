name: Deploy

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

concurrency:
  group: deploy-main
  cancel-in-progress: true

jobs:
  deploy-to-ec2:
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    permissions:
      contents: read

    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: ${{ secrets.EC2_PORT || '22' }}
          script_stop: true
          command_timeout: 30m
          script: |
            set -euo pipefail
            export APP_DIR="${{ secrets.APP_DIR }}"
            export APP_NAME="movie-wallet"
            export BRANCH="main"
            export HEALTHCHECK_URL="http://127.0.0.1:3000/api/health"
            export HEALTHCHECK_RETRIES="20"
            export HEALTHCHECK_DELAY_SECONDS="3"
            cd "$APP_DIR"
            chmod +x scripts/deploy_on_server.sh
            ./scripts/deploy_on_server.sh
